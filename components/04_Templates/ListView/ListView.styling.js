import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    ListViewRow : {
        flex: 1,
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'space-between',
    }
})

export {styles};
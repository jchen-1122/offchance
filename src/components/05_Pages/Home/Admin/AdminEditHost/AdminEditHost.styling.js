import { StyleSheet, Dimensions } from 'react-native'

const styles = StyleSheet.create({
    host_description: {
        color: 'gray'
    },
    host_text: {
        marginTop: 5, 
        marginBottom: 15
    }
})

export default styles

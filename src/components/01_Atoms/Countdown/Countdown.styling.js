import { StyleSheet, Dimensions } from 'react-native'

const styles = StyleSheet.create({
  Countdown: {
    fontWeight: 'bold',
  },

  Countdown_search: {
    fontSize: 12,
    fontWeight: 'normal'
  }

})

export default styles

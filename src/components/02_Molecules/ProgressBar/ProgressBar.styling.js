import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    view: {
        flexDirection: 'row',
    },
    raised: {
        fontSize: 20,
        fontWeight: 'bold'
    },
    goal: {
        paddingTop: 5
    }
})

export default styles
import { StyleSheet, Dimensions } from 'react-native'

const styles = StyleSheet.create({
    donor: { 
        width: 20, 
        height: 20, 
        borderRadius: 20 / 2, 
        marginRight: 10 
    }

})

export default styles
import {StyleSheet} from 'react-native';

const utilities = StyleSheet.create({
    flexCenter: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
    }
})

export {utilities};